model:
  name: RF_10
  version: "1.0.0"
  description: A classifier for raw log data using scikit-learn.

  backend: onnx
  caching: true
  model_args:
    model_file: log_classifier.onnx
    device: cpu

  
  input:
    type: array
    shape: [-1, 4]
    dtype: float32

  output:
    type: array
    shape: [-1, 1]
    dtype: int64

  preferred_batch_size: [128] ## preferred batch sizes for batching
  max_batch_delay_ms: 10 ## maximum delay to wait for forming a batch
  rush_batch_size: 256 ## choose based on system capacity, active when forward pass queue is linear
